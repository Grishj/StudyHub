import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ResourceViewerScreen = () => {
  return (
    <View>
      <Text>ResourceViewerScreen</Text>
    </View>
  );
};

export default ResourceViewerScreen;

const styles = StyleSheet.create({});
